using UnityEngine;
using UnityEngine.UI;

namespace Assets.Minijuegos.Scripts.Breackout
{
    public class PauseMenu : MonoBehaviour
    {
        [SerializeField]
        private Button pauseButton;

        private void Start()
        {
            //pauseButton.onClick.AddListener(PauseGame);
        }

    }
}